{
  "description": "model fallback list-when the primary model fails, the system will automatically try the fallback models",
  "fallbacks": [
    "gpt-4o-mini",
    "gpt-4-turbo",
    "gpt-3.5-turbo"
  ],
  "notes": [
    "the fallback list is sorted by priority",
    "the system will try the primary model and the fallback models in order",
    "if all models fail, an error will be returned"
  ]
}

