[project]
name = "mcp-workflow-backend"
version = "0.1.0"
description = "Backend for MCP Workflow Demo"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "uvicorn",
  "websockets",
  "openai",
  "mcp",
  "aiofiles",
  "pytest",
  "pytest-asyncio",
]
